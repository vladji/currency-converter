import Layout from "layout/Layout";

const Home = ({ location }) => {
    return (
        <Layout location={location}>
            <h2>Home page</h2>
        </Layout>
    );
};

export default Home;
